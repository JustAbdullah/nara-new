import 'package:flutter/material.dart';

class TestingAfter extends StatelessWidget {
  const TestingAfter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.amber,
      body: Stack(children: []),
    );
  }
}
